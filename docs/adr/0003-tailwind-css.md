# Tailwind CSS for styling

The site uses React and Svelte islands on the same pages. Tailwind utility classes are framework-agnostic — design tokens (colors, spacing, typography) are defined once in `tailwind.config` and shared across both frameworks without manual syncing. CSS Modules or vanilla CSS would require duplicating token definitions in each framework's component system.
